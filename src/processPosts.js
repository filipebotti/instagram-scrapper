const timeMatcher = /(\d{2})(h|H)(\d{2})?/;
const notShortDateMatcher = /(\d{2}) de ([a-zA-Z]*)/;
const shotDateMatcher = /(\d{2})[.|\/](\d{2})/;

module.exports = async (profile) => {
  const lives = [];

  profile.posts.forEach((post) => {});
};
